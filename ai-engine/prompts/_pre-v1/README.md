# Pre-v1 prompts (original, unversioned)

Superseded by the versioned templates in `prompts/v1/`. Kept for provenance.
The original `ai-engine/legacy/analyzer.py` embedded its own fallbacks and did
not depend on these files at runtime.
